# Shared environment variables
# This file contains variables that are shared across all environments (local-dev, test, prod)
# Currently, there are no shared defaults - all environment-specific variables are defined in their respective .env.{mode} files
