// To do: finish the implementation through a Cloud Function
// Deleting the old profile pic of a user Cloudinary after a change to a new one
export default async function deleteProfilePicFromCloudinary(publicId) {}
